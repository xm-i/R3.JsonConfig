# R3.JsonConfig - AI Agent Guidelines

当リポジトリ（R3.JsonConfig）においてAIエージェントがコードの生成、修正、およびレビューを行う際の規約を定めます。
※ ジェネレータの仕様やアーキテクチャの詳細は、重複を避けるため `AGENTS.md` には記述しません。必要に応じて `README.md` をお読みください。

## 1. コメント・ドキュメンテーション規約

当リポジトリにおける最重要ルールです。以下の規約を必ず遵守してください。

- **使用言語**: コード内のコメントはすべて「**日本語**」で記述すること。
- **XMLドキュメントの必須化**: 新規作成、または編集を行ったクラス・メソッド・プロパティには、必ず XML ドキュメンテーションコメント（`/// <summary>...`）を付与すること。

## 2. コーディング規約 (C#)

- **C# バージョン**: ターゲットフレームワーク（net10.0等）の最新 C# 言語機能を適切に活用すること。
- **Nullable 参照型**: プロジェクトは `#nullable enable` を前提としています。`?` や `!`、ならびに STJ 属性等を用いた適切な null 許容性のハンドリングを行うこと。
- **Source Generator 制約**: `R3.JsonConfig.Generators` (Incremental Generator) のコードを編集する際は、Roslyn API のパフォーマンス（不要なアロケーションの削減、Equatableなモデルの受け渡しなど）に配慮すること。

## 3. テスト規約

- テストフレームワークには **xUnit** を使用し、アサーションには **Shouldly** を用いること。
- 新たに機能を追加、または修正を行う場合は、`R3.JsonConfig.Tests` 側にテストケースが存在しているか確認し、挙動を保証するテストコードを必ず追加すること。

## 4. AI アシスタントとの対話規約

- **使用言語**: AI アシスタントとのチャットおよびすべての回答（実装プラン `implementation_plan.md` 等を含む）は**「日本語」**で行うこと。
